[package]
name = "renderer_sparse"
version = "0.1.0"
edition = "2021"

[dependencies]
rusqlite = { version = "0.32.1", features = ["bundled"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
plotters = "0.3.1"
reqwest = { version = "0.11", features = ["blocking", "rustls-tls"] }
