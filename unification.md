# Unification Document

1. All built binaries -> `bin` folder
2. OSM ma and map.html -> `map_data` folder
3. render cache -> `bin/cache_renderer` and `bin/cache_renderer_sparse` folders
4. yellow_icon -> `asset` folder
